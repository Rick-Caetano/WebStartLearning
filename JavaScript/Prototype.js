/*
    Prototype

    São funcionalidades que já foram criadas antes, logo as chamamos de prototipo. São colocados normalmente apos o ponto.
        * prototype-based language
        * prototype chain (cadeia de prototipos)
        * __proto__ (podemos olhar aqui os prototipos, ou seja, funcionalidades que ja estão disponiveis, e ai que está o segredo do JS)
        * 
         
        
*/

console.log("rick".length)
console.log(10.0.__proto__)
let array = ["a", "b"].length
console.log(array)